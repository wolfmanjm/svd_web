package assets

@haml list_fragment(p string) {
	%li= p
}

@haml TestIteration(peripherals []string) {
	.container
		%h1 Test iteration
		%ul
			- for _, p := range peripherals
				= @render list_fragment(p)
		%br
		done
}
